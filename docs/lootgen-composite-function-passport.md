# Составной лут — R8/R9

Версия 1.4.268: общий каталог/price reader, prepared graph ingress и GM-only durable подготовка результата в закрытом ChatMessage. Проверка каталога перед новым Item ingress подключена. Публикация того же результата, reference-only drag и окно выбора/выдачи улучшений подключены. Полный native multiplayer lifecycle ещё не подтверждён. Установка отдельных усовершенствований остаётся у R4/R7; подготовка graph не вызывает install API и ничего не записывает.

## Descriptor

`scripts/data/lootgen-item-descriptor.js`:

- `normalizeLootgenItemDescriptor(raw,{legacy=false}={})` возвращает detached v2 `{version,instanceKey,sourceType,sourceId,quantity,isBroken,upgrades,container}`. SourceType только gear/material/magicItem. IDs ограничены длиной и не содержат управляющих символов; quantity — positive safe integer. Составной host — quantity1, до3 upgrades с exact `{instanceKey,sourceId,slotIndex,choices}`, уникальными IDs/slots; choices валидирует общий R7 helper. Canonical container v1 разрешён только после полного обхода: shell identity, все оставшиеся children, quantity1 для составного узла, уникальные instance/container IDs, depth8 и cap200 документов. Private projectTree проверяет до normalize snapshot, чтобы нормализация не исправляла неверный запрос. Flat проверки object/id/exact/fail перенесены в lootgen-composition.js.
- `validateLootgenItemDescriptor(raw)` — strict delegate, не допускает лишние fields/ItemData/effects/цену в верхнем descriptor. Legacy boundary принимает только plain v1/без version, сохраняет количество/broken и строит детерминированный plain instanceKey; новые composition fields не теряются молча.
- `getLootgenAggregationKey(raw)` сохраняет прежний sourceType/sourceId/broken key для plain, а composed key включает физическую instance identity и composition. Два одинаковых улучшенных меча не объединяются.
- `projectLootgenDescriptorTree(raw)` возвращает readonly nodes `{descriptor,parentInstanceKey,currencyValue}` поверх одного detached canonical snapshot. `normalizeLootgenFlatItemDescriptor(raw,{legacy=false,allowContainer=false})` и `normalizeLootgenComposition(raw)` принадлежат scripts/data/lootgen-composition.js; прежний экспорт normalizeLootgenComposition из descriptor сохранён. Snapshot/value adapter зависят от flat owner без циклического импорта tree owner.
- Tests: `tests/lootgen-item-descriptor.test.mjs`, `tests/lootgen-container-value.test.mjs`.

## Варианты внутри бюджета

`chooseLootgenUpgradeVariant({host,remainingValue,form,catalogReader,manifest,random,createInstanceKey,attemptBudget={remaining:2000}})` в `scripts/data/lootgen-upgrade-variants.js` — pure helper existing generator. Reader `describeUpgradeHost(hostRow)` возвращает канонический R4 descriptor из trusted source data; `resolveValueComponent(component)` — прежний R4 value contract. Manifest предоставляет полную availability/profile, не клиентские произвольные эффекты.

Helper исключает candidate/unavailable, фильтрует type/rank, проверяет compatibility/capacity через validateUpgradeInstallation. Конечный pool без повторного выбора одной строки ограничен общим attemptBudget генерации. Выбор damageType из finite R7 whitelist сохраняется в descriptor. Полная цена считается evaluateItemValue; вариант принимается только если помещается в remainingValue. При неудаче остаётся обычный host. Возвращает `{descriptor,value,diagnostics}`; descriptor/value null при plain fallback. Физические IDs выделяются только после принятия варианта, через injected allocator. Не вызывает Foundry API.

`normalizeLootgenForm(raw)` дополнен enableUpgrades=false, upgradeChance=0..100 default0, maxUpgradesPerItem=1..3 default1, upgradeTypes (Материал/Зачарование/Проклятье), upgradeRanks (1..10). Пустые фильтры означают всё доступное, не разрешают unsupported profiles. Template catalog сохраняет эту же форму с прежней версией каталога2.

`generateLootgenResult(options={})` принимает эти поля либо form и зависимости manifest/catalogReader/createInstanceKey. Включённый режим допускает только известные неотрицательные safe prices; budget и quantity защищены от unsafe integer. Coin reserve вычисляется без промежуточного переполнения. После выбора base и broken marker helper оценивает variant до вычитания бюджета. Composed row содержит descriptor, quantity1, stackable=false и полную value/totalValue; следующая top-level попытка может выбрать такой же base с новой identity. Allocator проверяет uniqueness IDs во всём результате. Общая граница2000 попыток включает варианты и повторное заполнение; max40 top-level и max3 upgrades ограничивают materialized graph до160 документов, ниже cap200. Повторы plain stacks не создают дополнительные rows/documents.

Private `aggregateRows` переносит descriptor и использует getLootgenAggregationKey только для составных строк. Disabled branch сохраняет прежние rows/coins/spentValue и последовательность random; allocator не вызывается. UI передаёт enabled mode через trusted prepare route.

Tests: `tests/lootgen-composite-items.test.mjs`, `lootgen-generator`, `lootgen-template-catalog`, все lootgen regressions.

## Подготовленный graph

`buildCompositeItemGraph(descriptor,{buildBase,buildUpgrade,createDocumentId,manifest=[],actorId=""})` в `scripts/data/composite-item-graph.js` — async, возвращает `{rootItemId,documents,links}`. Callbacks предоставляют trusted detached ItemData из каталога, а не данные пользователя. Полный container descriptor отклоняется с container-requires-storage-planner: дерево собирает canonical StorageContainerItemService. Manifest обязателен для upgrades: отсутствие/несовместимость отклоняется. Полный набор слотов проверяется до первого вызова upgrade builder. IDs — новые уникальные Foundry16 alphanumeric, назначаются до materialization. Дополнительный actorId предназначен для известного target на стадии ingress preparation; callback buildBase обязан учесть descriptor.isBroken через свой trusted row context.

Private `cleanCatalogItem(source,id)` удаляет старые document ownership/ID/container/transfer receipts и применяет canonical buildHeldItemWornUpdate(false) к detached данным. Root quantity берётся из descriptor. Installed links полностью перестраиваются на новые child IDs; child quantity1, container/rootItemId, installedUpgrade hostActorId/hostItemId/slotIndex/category, canonical manifest profile и сохранённые upgradeChoices. Getter getItemUpgradeCategory и flags constants переиспользуются из существующего сервиса. Никаких world writes, случайных повторных установок и timestamps внутри builder. Явный custom profile у child отклоняется до подмены flags; input остаётся неизменным.

`cleanCatalogItem` не оставляет synthetic system.equipped на loot: dnd5e не хранит это поле. Tests: `tests/composite-item-graph.test.mjs`.


## Prepared ingress item

Владелец временного транспорта — scripts/data/lootgen-prepared-item.js. LOOTGEN_COMPOSITION_FLAG="lootgenComposition"; marker не является доказательством доверия.

- buildLootgenPreparedItem(descriptor,{graph,unitValue}) клонирует root, добавляет bounded runtimeItemGraph и exact marker {version:2,descriptor,unitValue,compositionKey}. Цена native root остаётся ценой основы; полная unitValue живёт только в транспортных данных.
- readLootgenPreparedComposition(itemData) возвращает null при отсутствии marker, иначе проверяет DTO, safe nonnegative value, canonical key, quantity1, число/уникальность IDs, root links и child sourceId/container/reverse link/choices. Повреждение даёт invalid-prepared-lootgen-item до записи. Private keyOf сохраняет прежний R8 JSON key для host-only; container key — sha256: + 64 hex от полного canonical descriptor/value. Private validatePreparedContainerGraph сопоставляет все hosts по storageContainerMember.composition, проверяет quantity, parent edges, portable snapshots/coins, все upgrade links/choices, отсутствие лишних nodes и совпадение внешнего root с graph root. fail создаёт этот error.
- buildInventoryIngressDescriptor получает полную цену и compositionKey. captureInventoryIngressIdentity добавляет key только composed quantity1, plain identity не меняется. InventoryIngressPlanner.preview/serialize выбирают version2 только для пакета с compositionKey, version1 для остальных. Private versionForRows/validWireIdentity и isValidSerializedInventoryIngressPlan проверяют exact v1/v2 поля, compositionKey<=8192 и наличие composed row в v2; parity сравнивает состав/choices/цену через key.
- getInventoryDismantleBlockReason(itemData) у inventory-ingress-descriptor возвращает причину для graph, host с установленными upgrades и installed child. resolveInventoryDismantleMinimumQuantity/Outputs возвращают null/[] для них. InventoryService.#executeDismantleInventoryItem показывает причину до journal/write: сначала снять upgrades. Независимый разбор одной оболочки не удаляет связанные предметы.

Tests: lootgen-prepared-item, inventory-ingress-descriptor, inventory-ingress-planner.

## Durable graph ingress

InventoryService.commitInventoryIngressBatch(request,{resolveRows,debitRow,grantContainer=null,allowPreparedLootgenGraph=false}) и private #commitLegacyInventoryIngressBatch принимают дополнительный **внутренний** source-adapter option, не wire payload. Только существующий trusted Chat grantBatch в main задаёт true. #assertInventoryIngressGraphSource допускает graph от storage либо lootgen+true+валидный prepared marker; generic direct ingress не получает этого разрешения.

#prepareInventoryIngressTargetReceipts сохраняет graphItemIds вместе с detached ItemData до create. #applyInventoryIngressTargetReceipt перепроверяет IDs и вызывает единственный materializeRuntimeItemGraph; root с graph не merge-кандидат. Journal хранит sourceOrigin/allowPreparedLootgenGraph, composition identity и graph source IDs. Source debit и claimed возможны только после всего graph. Terminal retry возвращает прежний outcome до чтения источника и не воссоздаёт удалённый после выдачи предмет.

buildRuntimeGraphDocuments(graph,operationId,rootData,{actorId=null,parentContainerId=null}) переназначает container/host/child IDs, а также installedUpgrade.hostActorId при известном target. Удаляет временные runtimeItemGraph/lootgenComposition flags: live value затем вычисляется штатно по реальным детям.

materializeRuntimeItemGraph(actor,graph,operationId,rootData,{recoverMissing=false,parentContainerId=null}) сохраняет прежнее поведение runtime transport по умолчанию. Prepared loot и durable storage owner разрешают recoverMissing: сверяет весь предоставленный снимок каждого уже существующего Item (private matchesPreparedValue допускает дополнительные defaults объекта, но сравнивает exact массивы и заданные значения, включая effects), затем создаёт только недостающие IDs. Чужая правка требует graph-manual-review без overwrite/повторного random. Write-then-throw подтверждается readback. Compensation здесь не удаляет partial graph автоматически; неоднозначность остаётся для сверки.

Tests: inventory-mutation-recovery (trusted adapter, journal-before-write, partial child, terminal deletion, dismantle), runtime-item-graph (foreign system/effects edits, default storage behavior), disarm-service regressions. Native QA testovyj3/CODEX: Foundry13.351+dnd5e5.2.5; actual embedded Items, partial root→child, exact retry, changed quantity rejection и правильный hostActorId подтверждены, все QA Actors удалены. Полная проверка GM Chat claim/публикации относится к следующей части R8.


## Общий каталог источников — 1.4.258

scripts/data/lootgen-source-catalog.js теперь владеет подбором кандидатов, ранее находившимся в LootgenApp:

- buildLootgenMundaneCandidate(gearItem,{rank,value,typeLabel,breakable=false}) сохраняет stable sourceId, package multipleAppearance и прежнюю plain stack policy; UI re-export сохраняет контракт импорта.
- buildLootgenGearTypeOptions(model,selectedState={}) исключает upgrade-категории и добавляет материалы как equipment filter.
- buildLootgenMundanePool({model,form,breakableGearIds=new Set()}) сохраняет rank/type/bargaining filters, цены resolveLootgenItemValue, package formula и breakable marker. buildLootgenMagicPool({form,documents=[]}) сохраняет stable magicItemId, rank/type/consumable metadata и прежний приоритет value → legacy priceGold → native price. Оба pure helpers сортируют rank/value без random. Private toNumber/toInteger/parsePriceToGold/normalizeBargainingTag/isBargainingBlocked перенесены из UI.
- readLootgenGearIndex() читает только world gear index и нужные flags/system.type/quantity для durability и upgrade compatibility/capacity. readLootgenMagicDocuments() читает managed magic pack, отсутствие pack даёт []. Ни create/sync, ни UI.
- LootgenSourceCatalog({getModel,getGearIndex=readLootgenGearIndex,getMagicDocuments=readLootgenMagicDocuments,getManifest=loadUpgradeAutomationManifest}) создаётся ровно один раз в main. load(form) параллельно читает нужные источники, нормализует форму и возвращает model/indices, pools, manifest/catalogReader только при enableUpgrades. Gear index также нужен для magic-only upgrades. generate(form,options={}) передаёт trusted pools/manifest/reader единственному generateLootgenResult; options не могут подменить эти зависимости.
- LootgenApp.#generateLoot() теперь вызывает общий service.generate; #getMagicDocuments/#buildGearTypeOptions — delegates. Старые #toValue/#getBreakableGearSourceIds/#buildMundanePool/#buildMagicPool удалены. generateFromForm сохраняет API. RebreyaMainModule.generateStorageLoot больше не создаёт Application: использует тот же сервис для plain rows. Composed storage form проходит shared prepared generation и canonical frozen snapshot (контракт1.4.270 ниже). openLootgenApp использует текущий cache key1.4.258 независимо от старой server manifest metadata.

Чистые type helpers normalizeLootgenTypeFilterKey/buildLootgenTypeFilterOptions/isLootgenTypeAllowed/resolveMagicLootgenTypeLabel и private cleanTypeLabel/parseMagicSignature перенесены без изменения поведения в scripts/data/lootgen-type-filters.js. Прежний UI path — только re-export; data больше не зависит от UI.

scripts/data/lootgen-catalog-reader.js: createLootgenCatalogReader({model={},gearIndex=[],magicDocuments=[],manifest=[]}) строит detached read-only maps stable IDs. Private index/values/flags/detached/id отвечают за снимки; duplicate identity отклоняется. resolveValueComponent(component) возвращает safe unitValue/priceKnown, canonical manifest upgradeProfile и пустой includedUpgradeSourceIds (каталог не объявляет bundled upgrades). Private modelPrice/magicPrice/present/validNumber/validValue различают неизвестную и явно нулевую цену; unsafe/negative values запрещены. Сохраняется прежний legacy priceGold приоритет, native denomination учитывает pp/gp/ep/sp/cp. Сломанное состояние не вводит новой скидки. Upgrade без managed source document имеет priceKnown:false. Явный сохранённый profile, отличающийся от manifest по canonical profileSignature, даёт UpgradeRuleError unavailable: он не подменяется правилами каталога. describeUpgradeHost также исключает сами upgrade templates.

describeUpgradeHost(component) использует buildUpgradeHostDescriptor на managed gear index либо native magic ItemData: категории/qualifier/capacity не угадываются по названию. Возвращает detached quantity1/unworn/unheld/unattuned descriptor. Host с уже сохранёнными installed links исключается из подбора новых вариантов до явного контракта bundled-price, чтобы не потерять прежний состав. Reader не пишет и не генерирует IDs.

Focused: lootgen-source-catalog, lootgen-catalog-reader, lootgen-type-filters, lootgen-app-context и generator regressions. Native testovyj3/CODEX: index745/manifest91/pool629; три отдельных kavaleriyskaya-pika с maloe-zacharovanie-ostroty, 100+3125=3225 за экземпляр и9675 всего; distinct upgrade instance keys. Обычное окно отрисовало сгенерированные строки/цены после extraction, viewport1292×920. Созданных Actor/Item/Chat нет; QA окно закрыто. Это не приёмка prepare-result/Chat claim.


## Сохраняемый результат мастера — 1.4.259

scripts/application/lootgen-generated-state.js:

- buildLootgenGeneratedState(form,{operationId,lootId,authorId},{catalog,buildItemData,prepareContainerGraph,createDocumentId,random=Math.random,now}) читает один catalog snapshot, запускает единственный generateLootgenResult, перепроверяет evaluateItemValue и строит detached ItemData/graphs через существующий builder. Allocator проверяет уникальные Foundry16 IDs во всём результате; rowId детерминирован из operationId/index. Каждая строка получает DTO, полную цену, names/decision/choices upgrades и claimed:false. Composed root не несёт legacy lootgenChat Item marker: обычный create-Item hook не должен отметить строку при появлении одной основы. Plain строки сохраняют прежний marker. World writes отсутствуют.
- readLootgenCatalogFingerprint(descriptors,snapshot) — pure signature referenced компонентов: safe price, compatibility/capacity, source name/type/rank/bargaining, _stats.modifiedTime, manifest decision/profile/capabilities. Source type namespaces разделены, повторные components дедуплицируются, сортировка детерминирована и не зависит от locale. Instance IDs не влияют. В reader gear index запрашиваются modifiedTime и upgrade/template flags. Этот helper подготовлен для повторной проверки перед claim; gate ещё не подключён.
- Private values/flags только нормализуют collections/metadata. Generated state содержит catalogFingerprint, rows, coins, spent/budget/count/time; второй world repository не создаётся.

scripts/application/lootgen-generated-result-service.js:

- LOOTGEN_PREPARE_RESULT_COMMAND="lootgen.prepare-result"; isValidPrepareLootgenPayload(payload) принимает exact {form}, exact normalized form с enableUpgrades:true либо enableFilledContainers:true, safe budget и JSON<=16384. ItemData/effects/лишние поля запрещены.
- LootgenGeneratedResultService({journal,buildState,findMessage,createMessage,activateMessage,coordinator}) — единственный workflow подготовки. prepare({operationId,form},{requesterId,authorId,assertAuthority}) сериализует operationId, связывает form/requester fingerprint и deterministic messageId/lootId. Private #assertMessage проверяет trusted envelope и exact receipt identity; #result возвращает detached current state, сохраняя claimed changes.
- Сначала ищет journal и существующий ChatMessage до random. Prepared journal сохраняет полный state до create; lost acknowledgment читается из сохранённой записи. После message-created preparedState очищается, сохраняется terminal receipt, затем разрешается generationReady. Retry после create/activation/journal ack failure не генерирует повторно; deleted completed message при сохранённом receipt требует ручной сверки. Гарантия использует existing journal retention и durable Chat state. Уникальные ID/чужой author/fingerprint/phase не перезаписываются. assertAuthority вызывается перед каждым write boundary после async reads/build.

Composition в scripts/main.js:

- Один service использует существующий InventoryService.mutationJournal и worldMutationCoordinator. GM-only typed route принимает exact {form,operationId}; внутренний form validator по-прежнему проверяет {form}. Передаёт payload operationId, authenticated requester и реального active-GM author/guard. Public API создаёт новый транспортный request ID для каждой попытки, сохраняя domain operationId, поэтому error cache транспорта не мешает persisted recovery. Public prepareLootgenGeneratedResult(form,{operationId?}) нормализует форму и использует этот route; возвращает {messageId,lootId,state}.
- #readLootgenMessage(messageOrId,{cloneState=true}) сохраняет прежнюю GM author verification и выдаёт detached envelope. Predicate поиска использует cloneState:false: он не копирует большие ItemData каждого сообщения, не изменяя raw state. #findLootgenChatMessage дополнительно исключает v2 drafts, пока generationReady/published не true. Это не даёт незавершённому draft попасть в прежний claim flow.
- #createLootgenChatDocument(state,{messageId,whisper}) — один canonical ChatMessage.create для прежнего createLootgenChatMessage и нового prepared flow. Подготовка использует непустой список GM whispers, поддерживает Foundry collections/Map.values, сохраняет deterministic ID через keepId. Пустой список получателей отклоняется до создания, не превращается в публичную запись.
- #activateLootgenGeneratedMessage(messageId) ставит readiness только после durable receipt, подтверждает update-then-throw readback. Публикации нет. buildLootgenChatContent для unpublished v2 показывает escaped неинтерактивный preview с именами/количеством/value/улучшениями/монетами; никаких claim/drag controls. Legacy content сохраняется.

Focused: lootgen-generated-result-service (retry, concurrency, authority, lost ack, wrong author, deleted result), lootgen-generated-state (full graph/value, profile/price/source stats signatures), group-command-dispatch (actual gateway GM/player/exact payload, canonical private publisher), lootgen-chat (draft escaping/no actions), composite-item-graph/lootgen-catalog-reader (custom profile rejection).

Native read-only QA testovyj3/CODEX: реальные buildLootgenItemData и каталог дали Алебарду3205 + Молот всадника3195, по2 graph nodes, total6400; legacy claim marker отсутствует. Повторное чтение каталога дало тот же fingerprint; modifiedTime доступен у всех745 gear entries. Сохранённый профиль реального шаблона совпадает с manifest. ChatMessage в живом мире не создавались; GM socket/publisher native и полная публикация/claim остаются открытыми.


## Проверка каталога и резервирование строк — 1.4.260

assertLootgenCatalogCurrent(state,catalog) в scripts/application/lootgen-generated-state.js пропускает legacy state; v2 требует сохранённую форму, fingerprint и strict DTO каждой строки. Fresh catalog.load и readLootgenCatalogFingerprint сравниваются без генерации/записей. Несовпадение цены, availability, profile или источника и повреждённый descriptor дают lootgen-result-stale с предложением новой генерации. Ошибка загрузки каталога сохраняется как retryable read failure, а не выдается за изменение правил.

Canonical Chat grantBatch в main вызывает gate из resolveRows({recovering=false}) перед новым inventory receipt. Recovery с существующим receipt пропускает повторную проверку каталога и использует сохранённые trusted ItemData; terminal inventory retry вообще не читает source. Каталог проверяется для всего сохранённого результата перед каждым новым пакетом Item; отдельные монеты не зависят от каталога. InventoryService остаётся владельцем определения recovering и target IDs.

LootClaimService.claimBatch перед новым claim проверяет пересечение доступных rowIds/includeCoins с nonterminal claims. Пересечение даёт lootgen-claim-in-progress до Chat/target writes; тот же claimId продолжает исходную операцию, независимые строки разрешены. После committed skip/failed rows снова доступны по прежнему контракту partial result.

Focused: lootgen-generated-state (legacy/current/price/availability/missing/malformed/read failure), loot-claim-service (prepared/granted reservations, independent item versus pending coins), group-command-dispatch (canonical grant adapter gate/recovery), inventory-mutation-recovery (partial graph with changed catalog guard, IDs and terminal replay).


## Выдача персонажу — 1.4.261

InventoryService.addLootgenRowToCharacterOnce(row,actor,mutationId,{allowPreparedLootgenGraph=false,beforePrepare=null}={}) сохраняет storage default и принимает внутреннее разрешение trusted v2 Chat adapter. Prepared composition без этого разрешения отвергается. Callback beforePrepare вызывается только до нового durable grant; ItemData клонируется из trusted state, graph marker сохраняется в journal, legacy lootgenChat Item marker удаляется. Private #executeInventoryGrantOnce дополнен beforePrepare/preparedLootgenFingerprint; fingerprint включает exact source data/quantity и Actor UUID. Kind, actorId/UUID, folder и fingerprint проверяются до terminal replay; новый получатель или source с прежним ID не допускаются. Persisted targetReceipt.graphItemIds проверяются перед canonical materializer; recoverMissing включается только для valid prepared graph. Storage graph default не меняется.

RebreyaMainModule.claimLootgenChatRowToCharacter(lootId,rowId,actorUuid,{operationId?}) отправляет exact typed lootgen.claim-character {actorUuid,claimId,lootId,rowId}, без ItemData/price. ClaimId — stable domain operation; каждый вызов получает новый transport request ID. Default domain ID включает requester/lootId/rowId/Actor UUID. Private #resolveLootgenCharacterDestination проверяет живого character Actor по canonical UUID resolver, OWNER либо GM и trusted ready v2 Chat; unpublished разрешён только GM. #findLootgenChatMessage(lootId,{allowDraft=false}) сохраняет прежний default и открывает ready draft только явному GM adapter.

Private #grantLootgenCharacterRow вызывается прежним LootClaimService.grantBatch для internal plan {destination:character,actorUuid,requesterId}; разрешает одну строку без coins, повторно проверяет источник/получателя/active GM и вызывает существующий InventoryService с mutationId lootgen-character:<claimId>. Catalog/ownership/authority beforePrepare не выполняются вместо persisted recovery. Source claimed пишется только после полного grant; prepared source claim резервирует строку при сбое. Public exact validation и destination в claim fingerprint блокируют смену получателя.

UI claimLootgenRowToSelf для v2 передаёт только references через API; локально Item не создаёт. Legacy self path сохраняется. Подготовленный draft без кнопок/drag; после публикации той же записи доступны проверяемые выдача и v2 drag route. Окно генерации поддерживает prepared v2; полная native приёмка остаётся открытой.

Focused: group-command-dispatch (OWNER/GM/exact/private access, grant-before-claimed, retry after failure, prepare retry through gateway without reroll), inventory-mutation-recovery (partial graph, changed catalog, target/source conflict, terminal replay, distinct hosts), lootgen-chat (references-only self click), disarm-storage (default storage regression).


## Публикация и перенос из Chat — 1.4.262

RebreyaMainModule.publishLootgenGeneratedResult(lootId) отправляет GM-only typed lootgen.publish-result с exact {lootId}. Private #publishLootgenGeneratedMessage(lootId,assertAuthority) использует ту же loot-claim:<messageId> queue, повторно проверяет trusted ready v2 state, раскрывает whisper:[]/blind:false и перерисовывает тот же document. Readback после ошибки проверяет и published flag, и фактическую видимость; частично применённое обновление повторяется, уже опубликованный результат возвращается без записи. Domain claim state/claimed rows сохраняются, генерация не вызывается.

#buildLootgenInventoryIngressRows теперь единственный row adapter и для preview, и для fresh grantBatch; v2 удаляет legacy lootgenChat Item marker из detached output. claimLootgenChatRow отвергает v2 до source mutation: source acknowledged только LootClaimService.

renderLootgenChatRow показывает экранированные названия upgrades и статус simple-implemented/existing-curse. bindLootgenChatMessage для ready published v2 формирует {type:RebreyaLootgen,version:2,lootId,rowId}, без ItemData/uuid; отсутствующие/claimed rows не перетаскиваются. registerLootgenChatHooks владеет одним дополнительным dropActorSheetData adapter: custom type всегда останавливает native drop, exact payload и character destination передаются в claimLootgenChatRowToCharacter. Неизвестные обычные типы остаются прежним hooks; invalid custom payload подавляется с уведомлением, без создания Item.

Focused: group-command-dispatch (publish before/after/partial fault, same document, GM access, legacy v2 rejection, stripped marker), lootgen-chat (safe drag, malformed custom payload, claimed source, escaped upgrade status), loot-claim-service и inventory-mutation-recovery regressions.


## Окно подготовленного результата — 1.4.263

RebreyaMainModule.getLootgenGeneratedResult(lootId) возвращает detached {lootId,messageId,state} trusted ready v2: draft только GM, published доступен игрокам. openLootgenGeneratedResult(lootId) проверяет доступ и вызывает существующий openLootgenApp({newWindow:true,sharedResult:{resultVersion:2,lootId}}). Последний теперь передаёт sharedResult также GM-окну и применяет его при reuse. Private #prepareLootgenInventoryIngress и typed inventory.ingress.lootgen допускают draft только для authenticated GM; #executeLootgenInventoryIngress(payload,{allowDraft=false}) получает разрешение из server context, не wire. Exact ingress schema допускает empty rowIds только при includeCoins:true; монеты идут тем же source claim с group-bound plan. Catalog gate вызывается только для Item rows.

Private #readPreparedLootgenClaimRequest(lootId,claimId,{rowIds=null,coinsOnly=false}) до fresh source/preview/current-group читает v2 claim fingerprint и восстанавливает exact validated payload с прежним планом и получателем. Изменённые requested rowIds/coinsOnly несовместимы. Public claimLootgenChatRowToInventory и claimLootgenChatAllToInventory используют этот replay; у all добавлен coinsOnly=false. Legacy plain behavior сохранён.

LootgenApp constructor/#getFormSnapshot/applyLootgenTemplate/_prepareContext включают enableUpgrades, upgradeChance, maxUpgradesPerItem, upgradeTypes/upgradeRanks; шаблоны сохраняют те же поля. #generateLoot сохраняет pendingPreparation {key,operationId} при ошибке и объединяет одинаковые in-flight requests; изменённая форма требует дождаться текущей генерации. Enabled mode вызывает prepare API, plain — прежний SourceCatalog.generate. Публичный generateFromForm возвращает detached result.

#projectPreparedResult(result) сохраняет отдельные rows, descriptor, totalValue, claimed и catalog fingerprint без legacy aggregation; adds upgradeSummaries/chat IDs. #normalizeSharedResult для v2 доверяет только живому getLootgenGeneratedResult(lootId), игнорирует caller rows/price; constructor/restoreGeneratedResult/setSharedResult/_prepareContext используют эту проекцию. #cloneGeneratedResult({referenceOnly=false}) сохраняет v2 state при чтении и только {resultVersion,lootId,hasResult} в clear undo. #buildSharedPayload даёт v2 references, automatic share после generation отключён для draft.

#addRowToInventory/#addCoinsToInventory/#takeAllToInventory используют trusted claim APIs; #claimPreparedResult(action,execute) сохраняет stable claim ID после ошибки, очищает после success/terminal partial, перечитывает source state, не заменяет уже открытый другой результат и не маскирует исходную ошибку ошибкой refresh. #sendResultToChat публикует тот же result. handleLootgenChatClaim и updateChatMessage hook обновляют v2 projection вместо legacy удаления строк. _onRender ограничивает numeric controls, сохраняет filters, блокирует повторный generate button/clear during generation; v2 cancel/no-op не даёт success status.

Chat UI: formatLootgenUpgradeSummary(upgrade) объединяет имя, localized damageType choice и automation status; HTML/Handlebars экранируют результат. Export openLootgenRowPreview(message,rowId,{baseOnly=false}) показывает v2 composition в DialogV2 по сохранённым данным, с отдельным открытием основы; random/Item creation при composition preview отсутствуют. buildLootgenChatContent(state,{showOpenWindow=true}) показывает GM reopen-only button в draft; composition preview отключает эту кнопку. bindLootgenChatMessage обрабатывает reopen через public API. Template lootgen-app.hbs загружается через штатный server template loader без query-параметров (Foundry проверяет расширение), имеет настройки, полную стоимость/состав и disabled claimed rows.

Focused: lootgen-app-context (form/templates, retry, separate composition/reopen, direct actions/coins/clear references), group-command-dispatch (private GM ingress/read, persisted-plan replay, coins-only source commit), lootgen-chat (composition preview without writes), все lootgen/domain/claim recovery regressions.

## Стоимость canonical контейнеров — основа R9, 1.4.264

`normalizeLootgenComposition(raw)` в lootgen-item-descriptor.js принимает exact metadata `{version:2,instanceKey,sourceType,sourceId,isBroken,upgrades}` через прежнюю проверку descriptor/choices. Quantity принадлежит row, дерево — только row.container; лишние поля отклоняются. Public normalizeLootgenItemDescriptor пока по-прежнему отвергает container: генерация и grant дерева ещё не подключены.

`readContainerValueNodes(snapshot,{shell=null}={})` в lootgen-container-value-adapter.js проецирует один canonical v1 scope в `{containerId,entries,currencyValue}`. При shell сверяет metadata с state.lootgenComposition; entries содержат detached composition, row.quantity и ссылку на единственный child snapshot. Reader не обходит children и не мутирует input. Пропускает claimedRowIds, требует unique row IDs/known composition; Journal/legacy unknown composition исключают budget candidate. Для оставшихся manualCoins/generatedCoins суммирует pp/gp/sp/cp с safe integer arithmetic, игнорирует derived totalCopper; unknown denomination/negative/fraction/overflow отклоняются. coinsClaimed обнуляет обе секции. Private object/fail/same валидируют shape/equality. createLootgenCatalogReader публикует этот reader рядом с прежними price/host методами.

StorageContainerSnapshot owner: normalizeItemRow сохраняет валидную composition и проверяет quantity до legacy coercion. normalizeContainerRow сверяет metadata строки и nested root, сохраняет sourceId каталога; buildStorageContainerRow переносит root metadata в row без второго дерева. normalizeSnapshot валидирует optional state.lootgenComposition. rekeyStorageContainerSnapshot меняет container IDs, сохраняя catalog sourceId/composition; legacy fallback sourceId остаётся storage-container:<id>. Portable flags сохраняют metadata. Capture/materialization со связями upgrades ещё требуют R9.3; не считать текущую основу готовой выдачей контейнеров.

`evaluateItemValue` создаёт единственный context `{instances,containers,documents}`; private evaluateValueNode(d,reader,context,depth) разделяет его на всех уровнях, включая upgrade IDs. Root depth0, контейнеры до8, общий cap200 Item documents; stacks считаются одним документом, composed/container quantity1. Resolve component получает detached host без дерева. Для каждого контейнера reader проверяет shell, evaluator проверяет container identity и прибавляет внутреннюю валюту/полные child totals к contentsValue. Unknown цены/состав, повторные identities, cycle/depth/count и overflow не возвращают частичную сумму. Unsupported-container сохраняется для reader без адаптера. Plain return shape не изменён.

Focused: lootgen-container-value (canonical 10000 budget example, nested/rekey, claimed/currency, shared identities/cycle, depth8/9, cap200 with upgrades, zero/unknown, mismatch/shape), helper makeValuedContainer строит настоящий snapshot и model-backed reader; storage-container-snapshot (exact metadata/portable/conflict/quantity); item-value и existing storage/lootgen/recovery regressions.

## Профили вместимости и физические ограничения — основа R9, 1.4.265

`resolveLootgenContainerProfile(catalogRow)` в lootgen-container-rules.js принимает native Item-like источник (либо row.itemData) и возвращает `{eligible,reason,capacity,weightlessContents}`. capacity имеет `{count,weightLb,volumeFt3}`; null — отсутствующий предел, явный0 — нулевая вместимость. Требуются container type и хотя бы один finite подтверждённый предел; malformed/unknown units/unsafe значения дают ineligible, никогда Infinity. Существующие dnd5e lb/tn/kg/Mg conversions совпадают с inventory-weight; ft3/cubicFoot/liter поддержаны для volume. weightlessContents читается из properties Array/Set. Private measure/object/numeric/fail проверяют и конвертируют значения.

`canFitLootgenContents({profile,currentContents=[],candidate})` проверяет все объявленные limits. Footprint имеет quantity, weight `{value,units}`, optional volume, upgrades массив физических weight/volume, isContainer/contentsWeight/weightlessContents. Это только одноуровневая проекция измерений, не сохранённое дерево. readLootgenPhysicalFootprint/private addMeasurement учитывают quantities, installed upgrades, occupied space и переносимый вес вложенного контейнера (содержимое только если не weightless). Composed/container quantity1. Невесомость родителя не отменяет его capacity. Unknown необходимое measurement/overflow отклоняются; count сравнивается точно, физические дробные измерения — с floating-point tolerance. По умолчанию отсутствие объёма даёт unknown-volume. Опция allowUnknownVolume допускает неизвестный объём только при известном weight/count limit, сохраняя проверки известных объёмов; volume-only профиль по-прежнему требует известные данные. Генерация использует этот предложенный пользователю вариант как явно объявленное рабочее допущение.

`debitLootgenBudget(remaining,amount)` возвращает разность только для неотрицательных safe integer и amount<=remaining; иначе RangeError. Общий recursive ledger ещё не подключён.

Catalog reader private physicalSource разрешает только authoritative gear/magic documents; `resolveContainerProfile(component)` делегирует pure profile, `readPhysicalItem(component)` возвращает detached `{type,system:{quantity,weight,volume,capacity,properties}}` либо null. readLootgenGearIndex дополнительно запрашивает system.weight/volume/capacity. Публичные мутации/world-state не изменены.

Focused: lootgen-container-rules (finite capacity/units/shape, occupied/upgrades/count, weightless nesting, volumes/unknown, overflow/immutability, exact debit), lootgen-catalog-reader (detached physical profile), lootgen-source-catalog (required index fields).

## Генерация canonical деревьев — 1.4.266

normalizeLootgenForm дополняет сохранённые templates defaults enableFilledContainers=false, filledContainerChance=0 (0..100), generationDepth=1 (1..3). generateLootgenResult при enabled filled mode проверяет safe budget, фильтрует authoritative prices с bounded unknown-price diagnostics и вызывает internal generateLootgenContainerResult в lootgen-container-generation.js. Disabled branch и его random/result сохраняются. Новые options — те же form fields и coinWeightPerCoinLb.

generateLootgenContainerResult({form,mundanePool,magicPool,catalogReader,manifest,random,createInstanceKey,batchId,generatedAt,pick,makeCoins,coinWeightPerCoinLb=0.02,priceDiagnostics=[]}) — policy helper единственного domain generator. Он не пишет документы/settings. Один ledger itemBudgetRemaining/coinBudgetRemaining/documentsRemaining/attemptsRemaining охватывает всё дерево. Резерв монет вычисляется один раз, глобальные cap200 documents с upgrades и2000 attempts; top-level itemCount отдельно, depth по форме. Private buildCandidate(allowance,depth,excluded) использует прежние weighted pick, multiple appearance, broken roll и upgrade variant helper. Вариант резервирует shell/upgrades; child получает ограничение item allowance и расходует общий ledger. Parent capacity проверяет каждый complete child footprint; rejected subtree возвращает item/coin/document reservations, попытки и уже выданные IDs не восстанавливает. Из rejected candidates одного parent исключается source identity, исключая зацикливание.

Canonical snapshot создаётся только при принятом contents/coins: state.lootgenComposition — shell metadata, ordinary row.composition — child metadata, row.container — единственное nested tree. Manual coins содержат только pp/gp/sp/cp, без UI label. Presentation сохраняет native itemSystem; unknown profile/пустое наполнение остаётся обычной quantity1 оболочкой. Каждый root/child проходит evaluateItemValue; итог проверяет tree totals+external coins=spentValue+currencyValue и B=totalValue+unusedValue. spentValue не включает ни внутренние, ни внешние монеты; currencyValue включает обе секции. Ordinary stacks сохраняют quantity; разные roots не aggregate. Дополнительные result fields currencyValue/totalValue/unusedValue/diagnostics, прежние direct grant IDs/hasResult сохранены.

Private key/composition проектируют source/metadata, allocate валидирует уникальный bounded ID, note ограничивает diagnostics200; reserve/restore меняют только три ресурсных поля ledger; fraction ограничивает child allowance, physicalWeight читает shared footprint. Random обязан возвращать finite [0,1). Unknown required weight/volume-only measurement отклоняет содержимое; известные weight/count и volumes проверяются, unknown volume отмечается volume-unverified. Footprint readLootgenPhysicalFootprint(item,{needWeight=true,needVolume=false,allowUnknownVolume=false}) теперь экспортирован, возвращает count/weightLb/volumeFt3/volumeKnown. Currency footprint quantity1/currency:true не расходует Item count, но расходует вес; malformed currency footprint отклоняется.

LootgenSourceCatalog constructor принимает getCoinWeight=readLootgenCoinWeight; load создаёт physical reader также для filled без upgrades, добавляет coinWeightPerCoinLb; generate передаёт его генератору. readLootgenCoinWeight() читает штатные dnd5e currencyWeight/metricWeightUnits и CONFIG encumbrance.currencyPerWeight, переводит metric weight в lb, возвращает0 при выключенном весе валюты. Не пишет settings/Actor. buildLootgenGeneratedState передаёт этот snapshot option и готовит filled graph через injected canonical storage planner.

Совместимость/граница: private preparationFingerprint(form,requesterId) у LootgenGeneratedResultService исключает только точные новые disabled defaults перед fingerprint, сохраняя старые R8 ready/pending receipts без reroll. Non-default/changed request по-прежнему конфликтует. InventoryService.buildLootgenItemData допускает row.descriptor.container только при allowPersistedItemData, валидном prepared marker, совпадающем descriptor и exact runtimeGraph; сырой root-only fallback запрещён. RebreyaMainModule.generateStorageLoot поддерживает filled/upgrade storage templates через canonical frozen snapshot. Prepared boundary и Item ingress для дерева подключены; Controls/preview подключены ниже; полный multiplayer lifecycle остаётся R9.4.

Focused: lootgen-container-budget (shared budgets/reserves, upgrade children/cap200, attempts2000, depth1/3, rollback capacity, known-only volume, nested weightless, duplicate shells, zero/unknown/unsafe, disabled stream, deterministic tree), container-rules, source-catalog (physical read/coin settings), generated-result-service (R8 receipt compatibility), template/generator defaults, inventory-mutation-recovery/group-command-dispatch (no root-only fallback).


## Каноническое дерево контейнера и durable grant — 1.4.267

Владелец: scripts/data/storage-container-item-service.js. Constructor принимает прежние scene/actor adapters и новые journal=null, coordinator=WorldMutationCoordinator, buildItemData=null, getManifest=async()=>[], createDocumentId=documentId. Main передаёт существующие InventoryService.mutationJournal/worldMutationCoordinator и catalog builders; нового world setting, socket route или materializer нет.

- prepareItemGraph(snapshot,{actorId="",createDocumentId=this.createDocumentId,buildItemData=this.buildItemData,getManifest=this.getManifest}={}) заменяет прежний private #materializeChildren. Нормализует canonical v1 snapshot, обходит только оставшиеся Item/container rows, пропускает Journal rows, ограничивает дерево 200 документами вместе с upgrades. Local allocate/appendHost/visit проверяют ID, уникальность composition.instanceKey и собирают один detached runtime graph. Новый catalog host строится через buildCompositeItemGraph; оболочка сохраняет исходные price/capacity/weight/source flags, получает portable snapshot и фактическую visible currency. Обычные contents привязаны к containing container, upgrades — к host. Snapshot shell metadata и storageContainerMember.composition сохраняют catalog identity отдельно от runtime IDs.
- Живой transport не пересобирает каталог. captureHostData(item,allItems,composition) проверяет обе стороны installed links, собирает host-only runtimeItemGraph с upgrade детьми, оставляет обычное содержимое только в snapshot rows. Shell ItemData находится в presentation.itemData, ordinary ItemData — в row.itemData. storageCapturedHost помечает транспортные данные без upgrades для выбора live source; planner удаляет этот маркер с выдаваемого host. Эффекты, пользовательские поля, названия, цена и профиль оболочки сохраняются. Снятые upgrades исключаются из composition; новый known upgrade получает стабильный native-upgrade UUID key. Некаталоговый состав остаётся физически переносимым, но лишается подтверждённой price composition. Broken/orphan links, stacked host с upgrades и непредставимые ordinary-host descendants дают storage-container-manual-review до removeItemTree.
- captureFromItem(item) использует только текущих native children, исключает shell upgrades из ordinary rows и не возвращает ранее извлечённые Items из portable snapshot. Для unopened legacy сохраняется прежняя reset-Journal логика. plainItemData удаляет runtime Document identity aliases и membership/mutation flags. Private captureCurrency(currency) проверяет целые неотрицательные значения и сохраняет стоимость native ep как 5 sp; storage vocabulary остаётся pp/gp/sp/cp. visibleCoins по-прежнему учитывает claimed balance.
- materializeToActorOnce(actor,snapshot,mutationId,{parentContainerId=null}) сохраняет signature. При injected journal вызывает #materializeDurable: queue по source containerId; receipt key storage-container:mutationId; fingerprint связывает actor UUID, parent и normalized snapshot. До первого Item write сохраняет phase prepared, graph, expectedItemIds/rootItemId/sourceContainerId. Pending source не выдаётся под другим ID. План remap сверяется с receipt перед каждым retry. Один keepId batch, partial повторяет только missing IDs. Ошибка сохраняет observedItemIds; удаление уже наблюдавшегося Item, чужая правка или конфликт требуют сверки, без удаления изменённых детей. После полной проверки journal.finish сохраняет terminal result. Pre-journal legacy root с тем же mutation ID без полного receipt требует сверки, без новой выдачи. Terminal retry возвращает существующий marked root даже после обычных пользовательских изменений; отсутствующий root не регенерируется. Standalone legacy adapter без journal сохраняет прежний rollback path; main всегда inject journal.
- buildRuntimeGraphDocuments и materializeRuntimeItemGraph принимают optional parentContainerId для внешнего родителя root; остальные parent/installed edges remap внутри дерева. План опускает пустой system.identifier, который dnd5e 5.2.5 заполняет при создании, и недопустимые loot system.attuned/equipped. Явный identifier и поля поддерживающих их типов остаются в строгой сверке. Количество, effects и installed links не ослаблены.
- removeItemTree до удаления проверяет, что captured snapshot проходит prepareItemGraph и может быть восстановлен; restoreItemTree и snapshot rekey используют этот же canonical transport. Промежуток между Item write и сохранением observed IDs всё ещё требует сверки при недоказуемом удалении/сбое; полноценные active-GM/claim/source-debit сценарии остаются отдельными release checks.

Focused: tests/storage-container-item-service.test.mjs (catalog graph, live capture/restore, pending reservation, partial restart, changed/deleted children, terminal deletion, coins), runtime-item-graph.test.mjs (native schema, parent remap, strict recovery), storage-main-registration/module-manifest/main-composition-root (wiring/cache), существующие storage/lootgen/inventory recovery suites. Native testovyj3/CODEX, Foundry13.351+dnd5e5.2.5: фактические 3 Item (сундук + молот + зачарование остроты), capture даёт одну contents row, restore создаёт 3 Item с правильным hostActorId и 3 gp; QA Actors удалены. Полный пользовательский R9 generator→Chat claim пока не включён.

Native partial QA (те же версии и пользователь): первый keepId batch из 3 сохраняет только root; новый экземпляр сервиса с тем же journal создаёт только оставшиеся 2, pending=0. Receipt запоминает 1 observed ID; удалённый terminal root отклоняет повтор с storage-container-manual-review. Все временные Actor удалены.


## Подготовка дерева и компактный socket reply — 1.4.268

- buildLootgenGeneratedState принимает filled-only и filled+upgrades. Для container вызывает prepareContainerGraph(snapshot,{createDocumentId:allocate,buildItemData,getManifest:async()=>snapshot.manifest}), который main направляет в существующий StorageContainerItemService.prepareItemGraph. Один allocator и immutable manifest действуют для всего результата. Сохраняет полный validated prepared ItemData graph, currencyValue/totalValue/unusedValue/diagnostics; native root price остаётся shell price. Legacy lootgenChat Item marker отсутствует у всех composed roots, включая container без собственных upgrades.
- readLootgenCatalogFingerprint(descriptors,snapshot) обходит projectLootgenDescriptorTree и включает цены/availability всех descendants. Для tree signature version2 добавляет readPhysicalItem (weight/volume/capacity/properties) и coinWeightPerCoinLb. R8 host-only signature version1 остаётся совместимой. assertLootgenCatalogCurrent допускает filled-only и проверяет до first write; persisted recovery по-прежнему не перечитывает изменившийся каталог.
- sha256Hex(string) в scripts/shared/sha256.js — синхронный UTF-8 SHA-256 для sync readLootgenPreparedComposition/inventory projection, без внешней зависимости. Private rotate и constant K — compression primitive. Проверен через node:crypto для пустого ввода, Unicode, всех padding boundaries 0..129 и длинной строки. Полный descriptor остаётся в trusted Chat state; wire compositionKey для контейнера всегда 71 символ. Identity digest не заменяет sender authorization или trusted source lookup.
- Typed lootgen.prepare-result execute и private #publishLootgenGeneratedMessage возвращают только {lootId,messageId}; полный state больше не попадает в 65536-byte socket envelope. Public async prepareLootgenGeneratedResult/publishLootgenGeneratedResult сохраняют API {lootId,messageId,state}: private #hydrateLootgenGeneratedResult(reference,{published=false}) перечитывает доступный trusted ChatMessage, проверяет matching message ID/generationReady/published, до 50 раз с интервалом50ms. Репликационная задержка даёт lootgen-result-replication-pending; автоматического повторного запроса/random нет. UI сохраняет исходный preparation ID при ошибке.
- LootClaimService.claimBatch для нового resultVersion2 claim проверяет все запрошенные rowIds по top-level state.rows до journal/grant; preview child ID, unknown ID и parent+child отвергаются invalid-lootgen-row, включая coins в том же запросе. Terminal exact retry сохраняет прежний receipt outcome; legacy v1 filtering не меняется.
- InventoryService.buildLootgenItemData и existing character/party graph ingress принимают trusted prepared container целиком. Row.container не создаёт вторую tree representation: graph приходит в ItemData, canonical nested tree остаётся descriptor.container. Source debit/claimed только после полного graph; partial write сохраняет receipt IDs и восстанавливает missing nodes. Обычный raw descriptor не становится ItemData автоматически.

Tests: lootgen-item-descriptor, lootgen-prepared-item (200 docs, corrupt/missing/unlisted nodes, coin/parent drift), lootgen-generated-state (filled-only, one budget, nested catalog drift), lootgen-generated-result-service, group-command-dispatch (100KB state → compact reply, delayed replication without repeated mutation), loot-claim-service (top-level only), inventory-mutation-recovery (character+party partial tree), sha256. Native testovyj3/CODEX Foundry13.351/dnd5e5.2.5: real catalog sunduk+molot-vsadnika, generated prepared state263874 bytes, 38 Item documents, 71-character identity; native grant→capture value5003 unchanged, whole budget10000. Temporary Actor deleted. Этот тест не публиковал ChatMessage и не подтверждает multiuser socket/Chat lifecycle.


## Контейнер в окне и карточке лута (1.4.269)

- LootgenApp constructor/#getFormSnapshot/applyLootgenTemplate/_prepareContext сохраняют enableFilledContainers, filledContainerChance (0–100), generationDepth (1–3). #generateLoot вызывает существующий prepare-result для upgrades либо filled-only и сохраняет operationId при сетевом повторе. #projectPreparedResult читает trusted state, добавляет containerSummary и отдельно исходную internalCurrencyValue до обнуления забранных внешних монет. Change handler нормализует числовые поля. UI не записывает world settings.
- buildLootgenGeneratedState добавляет контейнерной строке плоский compositionValues: instanceKey, baseValue, upgradeValue, contentsValue, currencyValue, totalValue (и diagnostics вычислителя). Цены вычисляются из того же snapshot каталога при подготовке. contentsValue исключает собственные монеты узла, но включает полную цену дочерних узлов; сумма пяти составляющих не дублирует валюту. Единственное дерево по-прежнему descriptor.container; price projection не используется для выдачи/переоценки.
- scripts/ui/lootgen-container-preview.js: projectLootgenContainerPreview(row) проверяет prepared graph и сохранённую разбивку, возвращает nodes с parentId/depth/name/quantity/upgrades/breakdown, totalValue/containedQuantity/upgradeCount. Только чтение saved descriptor/runtime graph, без catalog/random/Item. private plural/amount/fail проверяют/форматируют данные. getLootgenContainerSummary(row) возвращает краткое число вложенных предметов и всех усовершенствований; renderUpgrade локализует saved damageType и экранирует имя; renderLootgenContainerPreview(row) строит раскрываемое HTML-дерево и пять ценовых составляющих. Старый результат без projection показывает дерево и предупреждение об отсутствующей разбивке, не выдумывает цены; противоречивая projection отклоняется.
- openLootgenRowPreview(message,rowId,{baseOnly=false}) для контейнера открывает тот же DialogV2 с деревом, кнопками оболочки и закрытия. renderRow/renderLootgenChatRow/buildLootgenChatContent добавляют escaped summary только корневой строке; дочерние nodes не имеют claim/drag/actions. Существующие root actions и trusted claim API сохранены.
- templates/lootgen-app.hbs: настройки наполнения/глубины, краткий состав, раздельные значения предметов, внутренних и внешних монет. styles/main.css: scoped responsive дерево и прокручиваемое окно. Composition root обновляет cache entrypoint, chat/generated-state и stylesheet до269; plain .hbs URL сохранён.

Focused: lootgen-app-context, lootgen-generated-state, lootgen-container-preview, lootgen-chat (filled-only retry, form/template round trip, saved price arithmetic, escaped tree, legacy missing breakdown, one root claim/no child actions).


## Генерация внутри хранилища (1.4.270)

- RebreyaMainModule.generateStorageLoot(form) сохраняет active-GM guard и legacy plain branch. Для enableUpgrades либо enableFilledContainers вызывает существующий buildLootgenGeneratedState с теми же catalog/buildItemData/prepareContainerGraph adapters и единым document allocator. ChatMessage не создаётся: результат возвращается каноническому StorageService.#openOnce для одного сохранения opened state на token/nested scope.
- Container row создаётся buildStorageContainerRow из capturePreparedContainer результата; root itemData берётся из frozen snapshot.presentation.itemData, value/totalValue сохраняются для строки. Ordinary row сохраняет composition, prepared itemData и explicit runtimeGraph для существующего storage ingress. Root descriptor container не дублируется рядом со snapshot.
- StorageContainerItemService.capturePreparedContainer(itemData) валидирует полный saved prepared marker/graph, создаёт detached read adapters с native IDs и вызывает общий private #captureItemTree(item,allItems). Этот метод выделен из captureFromItem(item); live capture и detached capture имеют одного владельца обхода, installed links, native currency, Journal rows, captured host metadata. Ни каталог, ни world Documents не изменяются. Повреждённый/неполный prepared graph отклоняется до создания storage row.
- Frozen snapshot содержит per-host данные/effects/runtime graphs. prepareItemGraph использует их и не реконструирует уже выпавшие вещи по изменённому каталогу. Root/children/upgrades получают новые взаимосвязанные IDs перед выдачей; существующий durable materializer отвечает за partial target recovery и source debit.
- StorageService и LootgenTemplateCatalog читают normalizeLootgenForm версии266, чтобы новые filled fields не терялись из-за старого browser import. Composition root cache-busts эти владельцы, StorageCommandService и StorageContainerItemService до270; поведение normalizers/commands не дублируется.

Focused: storage-container-item-service (freeze complete graph/effects/choices/native coins, invalid graph reject), group-command-dispatch (real composed generator → StorageService.open → saved rows → catalog-independent graph; lost write acknowledgement/reopen/new service without reroll, ordinary upgrade graph), storage-service/container-hierarchy/socket, module-manifest/storage-main-registration/lootgen-template-catalog.
