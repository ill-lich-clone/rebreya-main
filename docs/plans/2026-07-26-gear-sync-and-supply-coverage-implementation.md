# План реализации синхронизации снаряжения и покрытия припасов

> Реализация ведётся в `lich_branch`, отдельными осмысленными коммитами. Диагностический `Trace-20260724T044510.json` не входит в изменения.

## Задача 1. Зафиксировать контракт объединённого каталога тестами

Файлы:

- изменить `tests/gear-data.test.mjs`;
- изменить или создать `tests/gear-catalog-sync.test.mjs`;
- использовать фикстуры таблиц в `tests/fixtures/`.

Шаги:

1. Добавить тест: готовый продукт существует только при наличии базовой строки общего листа.
2. Добавить тест: профильная строка обогащает базовый продукт и не создаёт второй продукт.
3. Добавить тест: базовая цена и описание не перезаписываются профильным листом.
4. Добавить тест: продукт и материал с одинаковым названием остаются разными сущностями.
5. Добавить тесты ошибок для неоднозначной связи и профильной строки-сироты.
6. Запустить новые тесты и убедиться, что они падают по ожидаемой причине.

## Задача 2. Построить единый нормализатор источников снаряжения

Файлы:

- изменить `scripts/data/importer.js`;
- изменить `tools/import-gear.ps1` либо заменить его тонким вызовом единого импортера;
- при необходимости создать `scripts/data/gear-catalog-normalizer.js`;
- обновить `data/gear.json`.

Шаги:

1. Нормализовать строки общего листа в базовые продукты со стабильными идентификаторами.
2. Построить индексы по каноническому ключу, идентификатору, имени и материалу.
3. Обобщить существующий подход `mergeGearWithImplants` для типизированных расширений.
4. Добавить объединение оружия и огнестрела.
5. Добавить блок `upgrade` из листа усовершенствований.
6. Сохранить существующее объединение имплантов через тот же контракт.
7. Генерировать структурированный отчёт потерянных, неоднозначных и осиротевших строк.
8. Прерывать генерацию при ошибках целостности.
9. Запустить тесты каталога.

## Задача 3. Исправить и проверить связи готовых предметов с материалами

Файлы:

- изменить нормализатор каталога;
- обновить `data/gear.json`;
- изменить тесты данных снаряжения и материалов.

Шаги:

1. Разрешать `predominantMaterialId` после загрузки актуального каталога материалов.
2. Не смешивать идентичность материала и готового продукта при совпадении названий.
3. Добавить тест для `Сухожилие чудовища`.
4. Добавить тест для `Серебрение оружия` → `Серебро`.
5. Проверить, что все именованные и однозначно разрешимые материалы получают идентификатор.
6. Вывести оставшиеся неразрешённые связи в отчёт.

## Задача 4. Пронести механику усовершенствований в документы Foundry

Файлы:

- изменить `scripts/data/gear-compendium.js`;
- изменить нормализацию модели снаряжения;
- изменить тесты компендиума снаряжения.

Шаги:

1. Добавить `upgrade` в нормализованный объект снаряжения.
2. Добавить блок в управляемые flags и сигнатуру синхронизации.
3. Отобразить читаемую механику усовершенствования в описании документа.
4. Не изменять существующие owned Items персонажей.
5. Проверить стабильность `_id` при обновлении управляемого документа.
6. Проверить удаление дубля только внутри управляемого gear-компендиума.

## Задача 5. Добавить состояние покрытия расходов группы

Файлы:

- изменить модель/сервис группового инвентаря;
- изменить `scripts/ui/inventory-app.js`;
- изменить шаблон группового инвентаря и стили;
- изменить тесты инвентаря и разрешений.

Шаги:

1. Добавить независимые булевы поля покрытия еды и воды со значением `false` по умолчанию.
2. Добавить авторизованную мутацию через существующий сервис и сокет.
3. Разрешить мутацию GM и текущим владельцам группового склада.
4. Добавить две галочки в секцию припасов группового инвентаря.
5. Заблокировать UI для пользователя без прав.
6. Добавить тесты сохранения, авторизации и независимости переключателей.

## Задача 6. Применить покрытие ко всему дневному циклу

Файлы:

- изменить расчёт снимка групповых припасов;
- изменить координатор перехода календаря;
- изменить итоговое сообщение в `scripts/ui/inventory-app.js`;
- изменить соответствующие тесты.

Шаги:

1. При покрытии еды возвращать нулевой эффективный расход еды.
2. При покрытии воды возвращать нулевой эффективный расход воды.
3. Использовать эффективные расходы в прогнозе дней.
4. Использовать их в предварительном просмотре перехода.
5. Не списывать покрываемый ресурс при фактическом переходе дня.
6. Сохранить обычное списание второго ресурса.
7. Проверить итоговые суммы и сообщение перехода.

## Задача 7. Полная проверка и публикация

Шаги:

1. Запустить целевые тесты после каждой задачи.
2. Запустить полный доступный набор тестов.
3. Запустить синтаксические/статические проверки проекта.
4. Проверить `git diff --check`.
5. Проверить итоговый diff и отсутствие изменений Actor/owned Items.
6. Сделать отдельные осмысленные коммиты реализации.
7. Запушить `lich_branch` в `origin` без force push.
